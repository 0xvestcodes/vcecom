import { StoreSettingsRefactored } from "@/components/settings/store-settings-refactored";

/**
 * Store settings page - Server component
 * Delegates all client-side logic to StoreSettingsRefactored component
 */
export default function StorePage() {
  return <StoreSettingsRefactored />;
}
