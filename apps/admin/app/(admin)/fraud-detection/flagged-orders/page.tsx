import { FraudFlaggedOrders } from "@/components/fraud-detection/flagged-orders";

/**
 * Fraud Detection - Flagged Orders Page
 * Shows orders flagged for fraud review and allows admin review
 */
export default function FlaggedOrdersPage() {
  return <FraudFlaggedOrders />;
}
